#include <stdio.h>

main()
{
  int c;
  char my_character[5];

  while ((c = getchar()) != EOF) {
    if (c == '\t')
      printf(%s, "\\t");
    if (c == '\b')
      printf(%s, "\\b");
    if (c == '\\')
      printf(%s, "\\\\");
  }
}
